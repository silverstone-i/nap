# ADR Index

Every architecture decision record in `docs/ADRs/`, with the scope it governs.
Scan this file when planning; read in full any ADR whose scope touches the work
at hand.

| ADR                                                            | Title                                                      | Status   | Date                                                  | Scope — read when working on                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------- | ---------------------------------------------------------- | -------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0001](0001-api-layering-and-module-structure.md)              | API layering and module structure                          | Accepted | 2026-07-26                                            | `apps/api/src` layer order and import direction (`util`, `db`, `services`, `middleware`, `framework`, `modules`, `scripts`); flat modules with schema-scope and licensable properties; per-module `apiRoutes/v1/` and `schema/migrations/`                                                                                                   |
| [0002](0002-technology-stack.md)                               | Technology stack                                           | Accepted | 2026-08-01                                            | PERN stack — Postgres 18, Express 5, React 19, Node 24 (`.nvmrc`); TypeScript in `apps/`; pg-schemata schema-per-tenant; JWT cookies + Redis; MUI 9 client; Vitest + supertest; compiler pinned `~6.0.3` (typescript-eslint range); ESM with `nodenext`, `target: es2023`, `strict`; dev loop `tsc --watch` + `node --watch`, no tsx/ts-node |
| [0003](0003-web-toolchain-vite-and-bundler-mode-typescript.md) | Web toolchain: Vite and bundler-mode TypeScript            | Accepted | 2026-08-01                                            | `apps/web` Vite 8 SPA; React 19, MUI Material 9, X Data Grid 9 on a shared major; `apps/web/tsconfig.json` with `moduleResolution: bundler`, extensionless imports, `jsx: react-jsx`, `noEmit`; build gated by `tsc --noEmit`; no per-app `typescript` dep                                                                                   |
| [0004](0004-portal-user-login-and-tenant-selection.md)         | Portal user login and tenant selection                     | Accepted | 2026-08-02                                            | `admin.portal_users` global identity; `admin.portal_user_tenants` bindings and `entity_id`; active-tenant resolution by `MAX(last_used_at)` over active tenants; login binding list; per-user active-tenant Redis key; `switch-tenant`                                                                                                       |
| [0005](0005-module-registry-migrations-and-seeding.md)         | Module registry, migrations, and seeding                   | Accepted | 2026-08-02                                            | migrator capabilities owned by pg-schemata, no NAP wrapper; scope filtering and `migrateAllTenants()` stay NAP-side; TypeScript migrations via `defineMigration` to `dist/`; module registry descriptor, two descriptors for dual-scope modules; `admin.countries` `char(2)` PK; seeds as `src/scripts/`; CI Postgres service container      |
| [0006](0006-tests-live-under-tests.md)                         | Tests live under `tests/`, never beside source             | Accepted | 2026-08-02 (corrected 2026-08-03, amended 2026-08-05) | all `*.test.*` files under each workspace's top-level `tests/`, mirroring the source tree; test type in the filename suffix (`foo.test.ts` unit, `foo.int.test.ts` integration) with a vitest project per type; `apps/api/tsconfig.build.json` emits only `src`; no colocated tests, no tests inside `src/`                                  |
| [0007](0007-rbac-self-contained-permission-cells.md)           | RBAC: self-contained permission cells                      | Accepted | 2026-08-07                                            | permission cell `(module, router, action) → { level, scope, visible_statuses }` as one row; `action = ''` router-wide cell, named-action override; cells evaluated whole, never decomposed                                                                                                                                                   |
| [0008](0008-rbac-resolution-any-cell-satisfies.md)             | RBAC resolution: any cell satisfies, grants never merged   | Accepted | 2026-08-07                                            | writes allowed iff one cell fully satisfies level, scope, and status; reads OR per-cell `(scope AND status)` predicates into a distinct union; no flat permission map — the cached artifact is the cell list                                                                                                                                 |
| [0009](0009-explicit-deny-deferred.md)                         | Explicit deny deferred: deny is absence of a cell          | Accepted | 2026-08-07                                            | v1 deny is the absence of a cell; no vetoes, no `level none` cells; adopting explicit deny or veto semantics later requires a new ADR                                                                                                                                                                                                        |
| [0010](0010-resource-splitting-instead-of-column-security.md)  | Resource splitting instead of column security              | Accepted | 2026-08-07                                            | no field-level permissions; audience-specific views and routers (`vw_project_cost`, `vw_project_profitability`, external routers with no internal columns); income-revealing fields banned from PM-readable resources; one scope granularity per router — company-level and project-level documents never share one                          |
| [0011](0011-rbac-schema.md)                                    | RBAC schema: roles, role_cells, entity_roles, cell_catalog | Accepted | 2026-08-07                                            | tenant-schema `roles`, `role_cells`, `entity_roles`, `cell_catalog`; role assignment joins `sources` via `entity_roles`, no `roles text[]`; every governed resource filterable by `project_id`/`company_id` or entity FK                                                                                                                     |
| [0012](0012-rbac-caching-and-staleness.md)                     | RBAC caching and staleness                                 | Accepted | 2026-08-07                                            | Redis permission canon `perm:<userId>:<tenantCode>`, TTL 900 s, lazy build; grant-mutation endpoints delete canon keys; JWT carries `ph` cell-list hash, never grants; `X-Token-Stale: 1` on hash mismatch                                                                                                                                   |
| [0013](0013-standard-resource-routes.md)                       | Standard resource routes                                   | Accepted | 2026-08-07                                            | `createRouter` 13-route contract at `/api/<module>/v1/<router>` — route names, methods, paths, level demands, per-route disable flags; custom routes declared before the factory; RBAC middleware on every route, audit injection on mutations; detail in `RULES/api-standard-routes.md`                                                     |

Superseded ADRs stay listed with their status and the ADR that replaced them.

Maintained by the `adr-index` skill — see `.claude/skills/adr-index/SKILL.md`.
