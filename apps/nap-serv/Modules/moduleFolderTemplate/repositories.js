'use strict';

/**
 * Example repository aggregation for a new module.
 *
 * Copy this folder when scaffolding a feature module and replace the placeholder
 * model imports with the actual implementations from `models/`.
 *
 * Each entry in the exported object should use a camelCase key and point to the
 * corresponding TableModel subclass. The file is imported by
 * `src/db/moduleRegistry.js` via dynamic import, so keep the export shape
 * consistent with other modules.
 */
// import ExampleModel from './models/ExampleModel.js';

const repositories = {
  // exampleModel: ExampleModel,
};

export default repositories;
